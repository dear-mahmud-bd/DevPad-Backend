# Development and testing dependencies (not installed in production image)
-r requirements.txt

pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
httpx==0.27.2          # AsyncClient for FastAPI test client
flake8==7.1.1
black==24.10.0
